function CardAction(props) {
    return (
        <>
        {id}
        {props.title}
        {props.img}
        {props.condition}
        {props.points}
        </>
    );
}

export default CardAction;